# nest-backend-boilerplate

Nestjs boilerplate code crafted by me. Sure to include all i need to scale fast.

## Testing Assumption

For evaluator and hiring-manager testing, rate limits are temporarily increased to 200 requests per minute across API throttle profiles (default, auth, and burst in production config). This reduces false-positive 429 responses during parallel UI/API validation.
